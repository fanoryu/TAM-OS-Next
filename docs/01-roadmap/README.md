# 01 — Roadmap

Where the project is going, and in what order. The roadmap is the single forward-looking source of
truth; the [Governance](../00-governance/README.md) layer above it is timeless and the
[Milestones](../05-milestones/README.md) layer records what has landed.

## Authoritative roadmap state

Current as of `main` after the **v2.10.0 official release** (release commit `335d53ed`).

| # | Item | Status |
|---|---|---|
| 1 | UX-006D — Presentation | **COMPLETE / FROZEN** |
| 2 | Readiness-1 — Employee Read Scope | **MERGED / FROZEN** (`3521d811`) |
| 3 | Readiness-2 — End-to-End User Journey Acceptance | **MERGED / FROZEN** (`580d8999`) |
| 4 | Readiness-3 — Release Candidate & Pilot Package | **MERGED / FROZEN** (`61ddd939`) |
| 5 | Controlled Pilot Sign-off | **APPROVED** (`df76ec20`) |
| 6 | **v2.10.0 Official Release** | **RELEASED / PUBLISHED / LATEST** — annotated tag `v2.10.0` on release commit `335d53ed`; asset `tam-os-v2.10.0.html`, 1,151,267 B, SHA-256 `60382271…2c7fa704`, byte-identical to `dist/` |
| 7 | Multi-User-0 — Shared Multi-User Architecture Decision | **MERGED / FROZEN** — architecture baseline only ([ADR-0003](../03b-repository-adr/ADR-0003-shared-multi-user-architecture.md) **Accepted** 2026-08-12). **Implementation NOT authorized** |
| 8 | **Controlled Pilot** | **NEXT — approved to start, NOT YET LAUNCHED** |
| 9 | Post-Pilot Findings & Remediation | **UPCOMING** |
| 10 | Pilot Exit Review | **UPCOMING** |
| 11 | Distribution-1 — Modular Distribution Migration | **UPCOMING / POST-PILOT** |
| 12 | Multi-User-1…8 — Shared Multi-User Implementation | **FUTURE — NOT AUTHORIZED, NOT STARTED** (blocked on the `CLAUDE.md` §4.3 amendment, which remains **unperformed**) |
| 13 | General-Use Readiness / Hardening | **FUTURE** |
| 14 | UX-006F / v3.0.0 | **FUTURE** |

**Sequence:** Controlled Pilot → Pilot Findings / Triage → Post-Pilot Remediation → Pilot Exit Review
→ Distribution-1 → **Multi-User-1…8** → General-Use Readiness → v3.0.0.

Four points the ordering is there to make unambiguous:

- **Released is not launched.** These are three distinct events, and only the first two have happened:
  ① **v2.10.0 publication — DONE**; ② **pilot sign-off — DONE**; ③ **pilot launch — NOT DONE.**
  Publishing made the verified artifact obtainable and checksum-verifiable; it did **not** start the
  pilot, and it is **not** a general-availability declaration.
- **Approved is not launched.** The maintainer has approved the controlled pilot and the artifact is
  published and frozen (`tam-os-v2.10.0.html`, 1,151,267 B, SHA-256 `60382271…2c7fa704`). **The pilot
  has not started and no launch date is set.** It launches only when the artifact is actually handed
  to the 1–3 named internal operators. Until then this milestone must not be marked
  active / live / in progress.
- **Distribution-1 does not block the controlled pilot.** Model A is retained for v2.10.0 per
  [ADR-0002](../03b-repository-adr/ADR-0002-canonical-distribution-architecture.md); Model B is the approved future
  architecture and its migration is post-pilot work with its own implementation and re-acceptance.
- **Neither v2.10.0 nor the controlled pilot is multi-user, and Multi-User-0 does not change that.**
  The released product runs on the **current local, single-device, trust-based** architecture; two
  computers running v2.10.0 hold **two independent datasets**.
  [Multi-User-0](Multi-User-0-Shared-Multi-User-Architecture-Decision.md) is an **architecture decision
  only**: ADR-0003 is **Accepted as the baseline**, but shared backend, shared database, real
  authentication, server-side authorization, server-side read scope and data migration are all
  **NOT IMPLEMENTED**, and implementation **has not started**. The two must never be conflated — the
  pilot's trust model is unchanged and must not be described as improved by a *planned* architecture.

Milestone-level detail lives in [Milestones.md](../05-milestones/Milestones.md).

| Document | Read it for |
|---|---|
| [Multi-User-0-Shared-Multi-User-Architecture-Decision.md](Multi-User-0-Shared-Multi-User-Architecture-Decision.md) | **Multi-User-0 — the shared multi-user architecture decision (ACCEPTED as the architecture baseline 2026-08-12; MERGED / FROZEN; implements nothing, and authorizes no implementation).** Mechanical inventory of the current persistence / authorization / read-scope / identity architecture and its trust boundary; the target model in which the **browser is untrusted and the server is the authorization boundary**; one shared company dataset with server-computed per-user projections (never one database per employee); single-company schema carrying `company_id`; the User/Employee/Membership identity model; weighted technology matrix (**Supabase + PostgreSQL + RLS + Supabase Auth, 4.55/5**, with Node+PG as the designated fallback); server-side authorization and read-scope design mapping the frozen 20 `ACTIONS` and the 6 `ENTITY_SCOPE` predicates onto RLS; actor-bearing audit model (today's audit record has **no actor** — a newly surfaced gap); concurrency rules; the **online-required** ruling; frontend KEEP/ADAPT/REPLACE/REMOVE classification; `localStorage` migration design (not executed); backup/PITR; deployment; the Distribution-1-first sequencing recommendation; the pilot-runs-in-parallel recommendation; cost model; **11 governance conflicts headed by the `CLAUDE.md` §4.3 client-only MUST**; and the proposed Multi-User-1…8 decomposition (**not authorized, not started** — blocked on the unperformed `CLAUDE.md` §4.3 amendment). Decision record: [ADR-0003](../03b-repository-adr/ADR-0003-shared-multi-user-architecture.md) (**Accepted**) |
| [Multi-User-Requirement-Note.md](../99-archive/roadmap-completed/Multi-User-Requirement-Note.md) | **NEW MAINTAINER REQUIREMENT — NOT YET ARCHITECTED.** The future multi-user target (shared company data, real authentication, backend/shared persistence, server-side authorization & read scope, multi-user deployment), why it is **separate from** the approved local/trust-based v2.10.0 controlled pilot, and the `CLAUDE.md` §4.3 amendment + ADR/SDR it would require first. Authorizes nothing; no technology chosen |
| [Repository-Freshness-Audit-2026-08-11.md](../99-archive/roadmap-completed/Repository-Freshness-Audit-2026-08-11.md) | The post-sign-off repository freshness audit — every stale, contradictory or superseded current-state statement found at `2d33b00f`, its classification, and the corrections applied |
| [Domain_Roadmap.md](Domain_Roadmap.md) | The sequenced evolution of the Domain layer |
| [Milestone_Roadmap.md](Milestone_Roadmap.md) | The high-level milestone track (Alpha → Omega) |
| [UX-004-Sidebar-Navigation-Discovery.md](../99-archive/roadmap-completed/UX-004-Sidebar-Navigation-Discovery.md) | UX-004 sidebar & navigation: discovery, approved design constraints, and phase sequence (**UX-004 is complete and merged**; this document is the historical discovery baseline) |
| [UX-005-Executive-Personal-Workspace-Architecture.md](../99-archive/roadmap-completed/UX-005-Executive-Personal-Workspace-Architecture.md) | UX-005/UX-006 product architecture freeze: Executive vs Personal Workspace model, KPI ownership, data-grid & Action Center contracts, and the UX-005A–F / UX-006A–E sequence. UX-005A–F all merged and the UX-005 Platform Freeze Review complete; sequence was resequenced (C=Design System, D=Global Search, E=Responsive/Density, F=Accessibility Hardening) |
| [MAINT-001-repository-maintenance.md](../99-archive/roadmap-completed/MAINT-001-repository-maintenance.md) | Repository maintenance & branding refresh: legacy-script removal, documentation sync, official branding adoption, and README refresh — **merged (core complete)**; §7 records the post-merge follow-up backlog (branding asset organization, README screenshot refresh, brand integration) |
| [UX-006-Identity-Personal-Workspace-Architecture.md](../99-archive/roadmap-completed/UX-006-Identity-Personal-Workspace-Architecture.md) | UX-006 **discovery / architecture baseline** (**UX-006 A–D is now COMPLETE and FROZEN**; this document remains the frozen architecture baseline, not a status page): identity/`currentUser` contracts, Personal Workspace & ownership model, roles/permissions & authorization enforcement, legacy v2.9.0 migration & schema strategy, backend-compatibility seam, invariants, test/verifier strategy, ADR-006-01…10, and the UX-006A–F decomposition. Extends the UX-005 Executive/Personal Workspace freeze. Owner decisions incorporated (§1A): provider-based local principal selection as an identity abstraction, and v3.0.0 ships both CEO + Employee principals — UX-006A planning is GO, implementation not yet authorized; ready for final owner review |
| [UX-006A-Identity-Foundation-Implementation-Plan.md](../99-archive/roadmap-completed/UX-006A-Identity-Foundation-Implementation-Plan.md) | UX-006A **implementation plan** (now **implemented & frozen**, merge `73096303`): exact file boundaries, `User` contract, `IdentityProvider` seam, single `currentUser` selector, CEO + Employee representative principals, fail-closed matrix, no-persistence decision (SCHEMA_VERSION stays 6), test/verifier plan, implementation order, acceptance criteria, and stop conditions |
| [UX-006B-Personal-Workspace-SelfScope-Implementation-Plan.md](../99-archive/roadmap-completed/UX-006B-Personal-Workspace-SelfScope-Implementation-Plan.md) | UX-006B **implementation plan** (now **implemented & frozen**, merge `f40fc064`, headless per amendment R1): the first identity→business-data boundary. Binds `User.employeeId → Employee.id`; **derived** Executive/Personal workspaces (no persistence, **SCHEMA_VERSION stays 6, no migration**); centralized SELF-scope resolver layer over unchanged raw resolvers; CEO/ALL_COMPANY preserved; GS/DG engines byte-identical (26/36); live GS scope wiring deferred to UX-006D |
| [UX-006C2-Mutation-Enforcement-Implementation-Plan.md](../99-archive/roadmap-completed/UX-006C2-Mutation-Enforcement-Implementation-Plan.md) | UX-006C2 **mutation-enforcement plan** (**FULLY IMPLEMENTED, MERGED & FROZEN** — C2A/C2B/C2C-1…C2C-4 all landed; `ACTIONS` 20; the user-reachable mutation-enforcement inventory is CLOSED): source-grounded map of every business mutation boundary (employee/contract/payroll/overtime/finance/import/supplemental/settings/bank/reset) with its persist + audit calls and a **bypass map** (bulk `worksheetSave`, generic `setOvertimeStatus`, destructive reset, repository seam). Applies the frozen `can(action, resource?)` at the **domain-mutation boundary** (never storage) so **denied ⇒ zero side effect (SE-0)**: no State/persist/audit/downstream. CEO pass-through (zero regression); Employee deny-by-default except own-Draft overtime self-service; Q-OT adds three narrow `overtime.*SelfDraft` actions (reviewed C2B amendment); ownership-change protection; no persistence/schema change; GS/DG/UI frozen. Staged **C2A (HR) → C2B (overtime) → C2C (operational)**; includes side-effect test methodology, verifier strategy, and stop conditions. **C2A is implemented, merged & frozen** (merge `a7369447`; Employee + Contract CRUD/update boundaries enforce SE-0; operational Contract paths `transitionContractStatus`/`renewContract` deferred to C2C). **C2B (overtime + ACTIONS 13→16) is implemented, merged & frozen** (merge `023a8214`; Employee own-Draft create/update/delete + Draft→Submitted, company management CEO-only, ownership/status-attack protection, bulk `worksheetSave` gate, duplicate-as-create, SE-0). **C2C plan frozen** (merge `9b36fc6`); **C2C-1 (Contract ops + Payroll) is implemented, merged & frozen** (merge `c15a7ad`; renewal + payroll→finance commit composite-atomicity, SE-0). **C2C-2 (Finance + Import) is implemented, merged & frozen** (merge `9ab256a`; ACTIONS 16→17 per the frozen Decision F2: adds `finance.manage`; SE-0 at all seven Finance administrative boundaries + execute + import commit). **C2C-3 (ACTIONS 17→20) and C2C-4 (zero new actions) are implemented, merged & frozen** (merges `88eb9ca`, `888d0a8`) — see the [C2C-3/4 mutation inventory & mapping proposal](../99-archive/roadmap-completed/UX-006C2C-3-4-Authorization-Mutation-Inventory-and-Mapping-Proposal.md) |
| [UX-006C-Authorization-Implementation-Plan.md](../99-archive/roadmap-completed/UX-006C-Authorization-Implementation-Plan.md) | UX-006C **authorization discovery / implementation plan** (frozen; **UX-006C1 implemented**, merge `27aa882`): *which actions a principal may perform on in-scope records* — strictly separate from UX-006B scope. `principalType` as policy subject (no RBAC); grounded `entity.verb` action vocabulary; centralized `ACTIONS` + `POLICY` table with `can(action, resource?)` / pure `canPrincipal(...)`; Employee **deny-by-default** with the single approved `overtime.submitSelf`; CEO preserved (pass-through); defense-in-depth **scope precondition** (AZ-1); fail-closed (AZ-2); enforcement at mutation boundaries (not UI-only); **no persistence, no schema change, no real auth, no GS/DG engine change**. Recommends staging into 006C1 (foundation) / 006C2 (mutation enforcement) / 006C3 (integration freeze); includes capability matrices, file/test/verifier plans, and flagged product questions. **Owner review complete; UX-006C — Authorization is COMPLETE and FROZEN in full** (C1 + C2 + C3) |
| [UX-006D1-Reachable-Principal-Selection-Implementation-Plan.md](../99-archive/roadmap-completed/UX-006D1-Reachable-Principal-Selection-Implementation-Plan.md) | UX-006D1 **reachable principal-selection plan** (now **implemented, merged & frozen**, merge `4a53a35`; `js/ui/identity-selector.js`): the minimal prerequisite that makes the existing UX-006A `IdentityProvider` principals *reachable in the running app* so live C2 enforcement can activate **without weakening fail-closed**. Roadmap **resequenced** to `C1 → D1 → C2A → …` because live `can(...)` enforcement is unsafe while `getCurrentUser() === null` is the only reachable state (would deny every mutation, regressing the operator). Compact **"Acting as"** native `<select>` in the persistent sidebar `.brand`; calls `LocalIdentityProvider.getAvailablePrincipals()` / `selectPrincipal(id)`; re-renders via the existing `render()` facade. **No default/implicit/boot CEO** (initial `currentUser === null`), **ephemeral** (no persistence, SCHEMA_VERSION stays 6, no `State.identity` slice), no auth/login/session wording, Global Search still deferred. Answers the 12 discovery questions (placement, adapter calls, re-render, null presentation, labels, ephemerality, a11y, mobile, no-auth wording, changed files, tests, how it unblocks C2). Implemented per this plan and merged; unblocked C2A, which is now implemented, merged & frozen (merge `a7369447`) |
| [UX-006C2C-Operational-Mutation-Enforcement-Implementation-Plan.md](../99-archive/roadmap-completed/UX-006C2C-Operational-Mutation-Enforcement-Implementation-Plan.md) | UX-006C2C **operational mutation-enforcement plan** (**FULLY IMPLEMENTED, MERGED & FROZEN** — C2C-1…C2C-4 all landed; `ACTIONS` 20): source-grounded map of every remaining operational boundary — operational Contract (`transitionContractStatus`, `renewContract`), Payroll (generate/override/lifecycle/**commit→finance**/prepare/lock), Finance (execute/archive/edit/manual), Import (`commitSmartImport`), Supplemental (create/refresh/account/notes/**post→finance**/recover), Settings (`saveSettings`, **destructive reset**), Bank/account, plus discovered paths (recurring, monthly-plan commit, legacy mapping, dedup merge). Applies the frozen `can(action, resource?)` at the **domain boundary** so **denied ⇒ SE-0**; **composite ops authorize once at the top** (renew→`contract.create`, commit→`payroll.manage`, post→`supplemental.manage`, import→`import.commit`) for atomic denial. Employee denied all; CEO pass-through (explicit D1); null deny. **Proposes NO ACTIONS change (stays 16)**; ambiguous mappings (finance CRUD, bank, recurring, monthly/legacy/dedup) flagged as **owner decisions**. Recommends staged **C2C-1 (Contract ops + Payroll) → C2C-2 (Finance + Import) → C2C-3 (Supplemental + System/Reset/Bank) → C2C-4 (remaining + freeze)** with per-domain harnesses, bypass map, atomicity tests, and a risk register; no schema/storage/GS/DG/UI change. **Plan approved & frozen** (merge `9b36fc6`); **C2C-1 implemented, merged & frozen** (merge `c15a7ad`). **C2C-2 (Finance + Import) is implemented, merged & frozen** (merge `9ab256a`); its Finance mappings were ruled by the owner as **Decision F2** (ACTIONS 16→17, add `finance.manage`) — see the [C2C-2 Finance mapping decision memo](../99-archive/roadmap-completed/UX-006C2C-2-Finance-Action-Mapping-Decision.md). **C2C-3/C2C-4 are implemented, merged & frozen** (merges `88eb9ca`, `888d0a8`): the governance review of C2C-2 proved this plan's inventory incomplete (`undoLastSmartImport` and `js/import/import-preview.js` were never inspected) — the corrected repository-wide inventory and mapping proposal is the [C2C-3/4 memo](../99-archive/roadmap-completed/UX-006C2C-3-4-Authorization-Mutation-Inventory-and-Mapping-Proposal.md) |
| [UX-006C2C-2-Finance-Action-Mapping-Decision.md](../99-archive/roadmap-completed/UX-006C2C-2-Finance-Action-Mapping-Decision.md) | UX-006C2C-2 **Finance action-mapping decision memo** (DECISION SUPPORT — NO IMPLEMENTATION): source-grounded inventory of the standalone Finance mutation surface — `executeTransaction` (irreversible posting = the frozen `finance.execute`) vs. reversible administration (manual create, `saveEditedTransaction`, `archiveTransaction`, `scheduleTransaction`, `cancelTransaction`, `duplicateTransaction`, inline delete). Shows `finance.execute` is architecturally specific (domain command + `TransactionExecuted` event), so overloading it (F1) misleads audit/least-privilege. **Recommends Decision F2: ACTIONS 16 → 17, add `finance.manage`** (CEO-only) for admin mutations; keep `finance.execute` for posting; `commitSmartImport`→`import.commit` unchanged; backup restore deferred to C2C-3 (`settings.manage`). Employee/null deny throughout; no code/authz/schema change here — **owner ruling required (F1/F2/F3)** before C2C-2 implementation. **Ruled F2, implemented, merged & frozen** (merge `9ab256a`). Governance review found this memo's "complete Finance inventory" claim inaccurate — `js/import/import-preview.js` was never inspected and `undoLastSmartImport` was never inventoried; both are corrected in the [C2C-3/4 memo](../99-archive/roadmap-completed/UX-006C2C-3-4-Authorization-Mutation-Inventory-and-Mapping-Proposal.md) |
| [UX-006C2C-3-4-Authorization-Mutation-Inventory-and-Mapping-Proposal.md](../99-archive/roadmap-completed/UX-006C2C-3-4-Authorization-Mutation-Inventory-and-Mapping-Proposal.md) | UX-006C2C-3/4 **mutation inventory & mapping proposal** (DECISION PREPARATION — NO IMPLEMENTATION): the corrected, repository-wide authorization inventory taken after C2C-2 merged. Enumerates **59** mutation boundaries across all 72 loaded modules (derived from every `State.*` write site **and** every persistence entry point, not from the UI): **25 already authorized**, **29 unauthorized and user-reachable**, the rest bootstrap/CLI/view-state. Classifies each by domain, reachability (LIVE / LEGACY BUT LIVE / TEST ONLY), blast radius and reversibility. Mandatory analyses: `undoLastSmartImport` (4-domain irreversible deletion, Employee-executable), legacy `import-preview.js` `confirmImport`/`applyMonthUpdate` (**LIVE, and reachable as a fallback from Smart Import**), month restore + `restoreCompleteBackup`, `resetAppData`/`startFresh`, supplemental, recurring, monthly plan, bank, settings, dedup merge, legacy mapping, payroll adjustments. Proposes **20 boundaries reuse existing actions** and **3 new actions** (`import.undo`, `data.restore`, `data.reset`) → **ACTIONS 17 → 20**; CEO allow / Employee deny / null deny throughout; no schema/storage change. **C2C-3** = irreversible/cross-domain lifecycle + import + restore (all new vocabulary), **C2C-4** = administrative domains (zero new actions) — and C2C-4 closes the user-reachable enforcement inventory. **Mapping APPROVED & FROZEN by governance review** (R1–R9 all ruled; `recurring.manage` and `bank.manage` rejected; one omitted boundary added as row 30, `markReviewed` → `finance.manage`; totals corrected to 60/30; the `finance.manage` description amendment recorded verbatim). Also records three out-of-scope outcome-reporting findings for a separate remediation pass. **C2C-3 (rows 1-9, ACTIONS 17→20) and C2C-4 (rows 10-30, zero new actions) are implemented, merged & frozen** (merges `88eb9ca`, `888d0a8`) — the user-reachable mutation-enforcement inventory is **CLOSED**. **UX-006C3 (Integration Freeze) is in decision preparation** — see the [C3 decision-preparation memo](../99-archive/roadmap-completed/UX-006C3-Integration-Freeze-Decision-Preparation.md) |
| [UX-006C3-Integration-Freeze-Decision-Preparation.md](../99-archive/roadmap-completed/UX-006C3-Integration-Freeze-Decision-Preparation.md) | UX-006C3 **integration-freeze decision preparation** (DECISION PREPARATION — NO IMPLEMENTATION): C3 was specified only as a one-line staging-table row, so this inventories the actual integration surface — **43 entries** (27 sidebar nav items, 12 Quick Actions, 4 Action Center generators) — and proves **every one is navigation-only**: no entry performs a domain mutation (`hrNavTo` assigns view state and re-renders; `State.view`/`detail*Id` are the in-memory category already ruled NOT APPLICABLE). Establishes that **destination availability ≠ action availability** (most views mix capabilities — `settings` alone spans `settings.manage`, `data.restore`, `data.reset`; `overtime` is Employee-authorized for own Drafts), so no one-action-per-destination mapping is defensible and **no new action is needed** (`ACTIONS` stays 20). Assesses VISIBLE+NORMAL / VISIBLE+DISABLED / HIDDEN and recommends a **split model** (navigation stays visible+normal; single-capability mutation controls may be visible+disabled; nothing hidden), a precise **C3 ↔ UX-006D ownership split**, render-time-derived availability (principal change already re-renders, so nothing may be cached or persisted), a **view-access-vs-mutation** deep-link ruling with no route guards, 11 proposed **Integration Freeze acceptance criteria** and a new behavioural harness. Confirms `AI_CONTEXT.md` / `ARCHITECTURE.md` are stale (both still say C2C "has not begun"). Eight questions (C3-R1…R8) — **ruled by Atlas**; decision preparation **MERGED** (`049ae0e`). **C3 implementation: IMPLEMENTED, MERGED & FROZEN** (merge `675cb314`) — machine-enforced integration manifest (`tools/integration-surface-manifest.js`) freezing all **43** surfaces with source↔manifest closure both ways, navigation stays visible+normal for CEO/Employee/null, seven single-capability mutation controls visible+disabled when denied via the shared `authzDisabled(action, resource)` helper delegating to the frozen `can(...)`, availability derived at render time (never cached/persisted), proven by `tools/verify-authz-integration-runtime.js` (91 checks); `ACTIONS` stays 20, `SCHEMA_VERSION` stays 6. **UX-006C — Authorization is COMPLETE and FROZEN in full; UX-006D is the current milestone** |
| [UX-006D2-Principal-Workspace-Presentation-Implementation-Plan.md](../99-archive/roadmap-completed/UX-006D2-Principal-Workspace-Presentation-Implementation-Plan.md) | UX-006D2 **principal & workspace presentation plan** (**implemented, merged & frozen**, merge `5163cfce`): the first purely presentational UX-006D phase, on the D2 baseline `de272283`. Records the **supersession** of the pre-C3 UX-006D staging-row authorization language (see [UX-006 architecture §20A](../99-archive/roadmap-completed/UX-006-Identity-Personal-Workspace-Architecture.md)) — *nav-per-principal*, *unauthorized routes blocked* and *employee never reaches exec routes/data* are **void as routing rules**, overtaken by the frozen C3 semantics. Fixes five observed presentation defects: the collapsed rail hid **which principal was acting**; the active **workspace context was never rendered** (headless since UX-006B); a denied control was **visually identical** to an ordinarily inert one (`opacity:.4`, and `#genPay` is disabled by a locked period too); Quick Actions looked like operational buttons; Action Center navigable rows were distinguishable only on `:hover`. Adds `#identityPrincipalContext` + a collapsed-rail chip with `data-principal-state`, a `data-authz-denied` presentation marker on the **unchanged** `can()` delegation, and additive CSS (authorized golden-master revision; `tokens.css` byte-unchanged). Distinguishes the **two causes of a null workspace** (no principal vs. no linked employee record). **Presentation only**: `ACTIONS` stays 20, `APP_VERSION` 2.9.0, `SCHEMA_VERSION` 6, `js/core/authz.js` untouched, no route guard, C3 closure green. Proven by `tools/verify-ux006d2-presentation-runtime.js` (127 checks). **Global Search scope wiring is OUTSIDE UX-006D** and stays deferred; **UX-006D3** (cross-surface consistency & acceptance) is next |
| [UX-006D3-Cross-Surface-Presentation-Implementation-Plan.md](../99-archive/roadmap-completed/UX-006D3-Cross-Surface-Presentation-Implementation-Plan.md) | UX-006D3 **cross-surface presentation consistency & acceptance** (**implemented, merged & frozen**, merge `e76460dc`): the final UX-006D phase and its acceptance gate, on baseline `5163cfce`. Inventories all 27 sidebar views plus detail views, Action Center, Quick Actions, sidebar, principal/workspace context, empty states and modals at three viewports — **33 PASS / 2 FIX / 2 DEFER**. **FIX 1:** `emptyState()` replaced the *entire page*, so nine views (Overview, Executive Insights, Cash Flow, Budget Center, Execution Center, Planned vs Actual, Compare Months, Monthly Trends, Reports) rendered an **untitled card** with no data — and lost the `.page-head` slot Quick Actions mount into, so Execution Center resolved 3 frozen Quick Actions but displayed **0**. The heading now derives from `PAGE_TITLES` (from the one `NAV_GROUPS` manifest), no call site changed, and *record not found* still renders none. **FIX 2:** a **pre-existing** 80px overflow of a 375px viewport in Release Notes, fixed by one additive `overflow-wrap:break-word` rule (`tokens.css`/`shell.css` untouched). Also **hardens** the raw-prose UX-005A dashboard guard into a comment-stripped label check **plus** an explicit UX-006 API-symbol check — strictly stronger, with regression proof. **Presentation only**: `ACTIONS` 20, `APP_VERSION` 2.9.0, `SCHEMA_VERSION` 6, `js/core/authz.js` byte-unchanged, 43 C3 entries and D2 semantics intact. Proven by `tools/verify-ux006d3-presentation-runtime.js` (84 checks). **DEFERRED:** disabled-reason discoverability on touch/keyboard (would change the frozen C3 disabled pattern) and principal-aware Global Search scope wiring (outside UX-006D). **After D3 merges, UX-006D = COMPLETE / FROZEN** |
| [Post-UX-006D-User-Readiness-Audit.md](../99-archive/roadmap-completed/Post-UX-006D-User-Readiness-Audit.md) | **Post-UX-006D user-readiness audit** (AUDIT — no runtime change), baseline `e76460dc`. Answers "what remains before real users?". **Headline finding: the UX-006B self-scope layer is fully built, fully tested, and connected to nothing** — `getScopedRecords()` has **zero production consumers**, so an Employee principal sees the whole company, including other employees' salaries, via lists, detail pages, deep links and Global Search (proven with a fabricated two-employee fixture). Mutation authorization is complete and correct; **read scope was never wired** — it fell between UX-006B (shipped headless per amendment R1) and UX-006D (presentation only). Not a security vulnerability (identity is a local, spoofable selector, explicitly *not* a security boundary) but a **P1 confidentiality/product defect** against the approved "self-only read" criterion (Decision Q2). **A CEO-only pilot is unaffected; an Employee-facing pilot is blocked.** Classifies every Employee-facing read surface, all workflows, six E2E journeys, persistence/recovery (SPR-079/082 intact), outcome integrity and a11y; recommends **2.10.0** (not 3.0.0 — Decision Q2 unmet) and the sequence Readiness-1 (read scope) → Readiness-2 (E2E) → Readiness-3 (RC) → pilot. 11 live backlog items, 4 rulings requested | 
| [Readiness-1-Employee-Read-Scope-Implementation-Plan.md](../99-archive/roadmap-completed/Readiness-1-Employee-Read-Scope-Implementation-Plan.md) | **Readiness-1 — Employee Read Scope & Privacy Closure** (**implemented, merged & frozen**, merge `3521d811`), baseline `3b78f854`. Closes the readiness audit's headline finding: `getScopedRecords()` had **zero production consumers**, so an Employee could read the whole company including colleagues' salaries. Wires the frozen UX-006B self-scope layer into People/HR lists and details, the payroll read funnel (worksheet, totals, cycle status, bulk eligibility, Payroll Health), overtime, adjustments, employee pickers, HR dashboards and every report row, the finance ledger and derived analytics aggregates, the Action Center payroll generator, the **breadcrumb terminal label**, and **Global Search** at its collector seam — the engine stays source-agnostic, so a foreign record is never *indexed*. Adds `getScopedRecordById()` so a detail id captured under another principal is re-scoped at render time, with out-of-scope and non-existent deliberately indistinguishable. `ENTITY_SCOPE` grows 4→6 (`payrollAdjustment`, `transaction`), each with an explicit predicate over an ownership field the domain already carries. Finance/Analytics follow the Atlas ruling (existing `employeeId` only; no invented ownership; navigation still visible+normal, no route guards). Canonical `State` is never narrowed and import/parser/persistence stay unscoped by design. **No new ACTION, no schema/storage change**: `ACTIONS` 20, `APP_VERSION` 2.9.0, `SCHEMA_VERSION` 6. Also closes **identity disclosure** per the later Atlas ruling — an employee's *name* is scoped data, so the overtime picker (reachable in the Employee-authorized own-Draft workflow), the overtime worksheet, contract/adjustment/legacy pickers, Duplicate Review render, Settings diagnostics, onboarding checklist and employee-naming alerts are all scoped, while `findEmployeeDuplicateGroups()` (integrity input) and the payroll **setup gate** (scoping it broke C3's visible+disabled controls for null) stay canonical and documented. Proven by `tools/verify-employee-read-scope-runtime.js` (**119 checks**) with a **negative control producing 49 counted failures** on the pre-Readiness-1 baseline, plus DOM proof in both artifacts. Next: **Readiness-2 — End-to-End User Journey Acceptance** |
| [Readiness-2-E2E-Acceptance-Implementation-Plan.md](../99-archive/roadmap-completed/Readiness-2-E2E-Acceptance-Implementation-Plan.md) | **Readiness-2 — End-to-End User Journey Acceptance** (**implemented, merged & frozen**, merge `580d8999`), baseline `3521d811`. Shifts the unit of validation from the boundary to the **journey**: can a real user finish the primary workflows with correct state, feedback, persistence, privacy and recovery? Eight journeys proven in the browser (real form submits, real modals, reload survival) **and** automated in `tools/verify-readiness2-e2e-runtime.js` (96 checks) — CEO finance (create→edit→schedule→execute, full history + audit + reload), Employee self-service **and** privacy in one run (own-Draft overtime persists; finance/lock denied SE-0; B never renders), payroll (generate→approve→**lock refuses posting**→unlock→post 2 *planned* txns linked both ways, never auto-executed), Smart Import commit→undo (pre-import backup, `import.commit`/`import.undo` audit, rollback removes exactly the batch), backup→restore→**Start Fresh** (forced backup, refuses wrong confirmation, clears every sensitive store), principal switching (CEO view byte-identical before/after — recomputed, not cached), settings (CEO persists, Employee denied through the same form), and supplemental from real overtime drift. **No product defect was found**; three near-misses were my own fixture/probe errors and are recorded, including an apparent settings authorization bypass that proved to be a probe at the persistence primitive rather than the form-handler boundary. Also **corrects the Readiness-1 `.gitattributes` recommendation**: the policy already existed and was correct — one stale CRLF worktree file was the whole cause, so no repo change was made. `ACTIONS` 20, `APP_VERSION` 2.9.0, `SCHEMA_VERSION` 6. Next: **Readiness-3 — Release Candidate & Pilot Package** |
| [Readiness-3-Release-Candidate-Implementation-Plan.md](../99-archive/roadmap-completed/Readiness-3-Release-Candidate-Implementation-Plan.md) | **Readiness-3 — Release Candidate & Pilot Package** (**implemented, merged & frozen**, merge `61ddd939`), baseline `580d8999`. Packages the frozen Readiness line as a controlled-pilot release candidate. **Version decision: 2.10.0 — *Governed Workspace*** (minor: materially new backward-compatible capability since 2.9.0; **not** 3.0.0 — no breaking change, and the roadmap's v3.0 criteria are met by earning a minor bump, not a major one), `SCHEMA_VERSION` **6** unchanged (no migration invented for a version bump), `ACTIONS` **20**. **RC identity lives in the paperwork, not the runtime**: `tools/app-version.js` rejects prerelease suffixes and derives the artifact filename from `APP_VERSION`, so the runtime says `2.10.0` and the release notes/README say "candidate, not published, not tagged" — enforced by new verifier guardrails that also preserve v2.9.0 as published history and require the trust-model caveat. Closes the two deferred acceptances through **real browser file I/O on both artifacts**: a genuine `.xlsx` `File` through the real `#fileInput` (parse → preview → production Smart Import builder → commit → pre-import backup → `import.commit` → undo → `import.undo`), and a real backup-file roundtrip (UI export → bytes → wipe → real `#importCompleteFile` → schema-6 preview → restore → persisted). **One product defect found and fixed (P2):** `smartRollbackPreview()` reported *created* counts as *removal* counts, so importing TAM's standard `Realisasi` workbook made the undo promise deletions it correctly did not perform — and write an `import.undo` **audit entry recording deletions that never happened**; retention is now computed once from the undo's own predicates and consumed by it, so reported and removed cannot drift. Removal behaviour unchanged. **First boot**: no auto-CEO, no persisted principal (none was added — no approved mechanism exists), fail-closed intact; only the null helper line's wording changed. **Disabled-reason discoverability**: audited across mouse/keyboard/touch — `title` works on hover only; accepted and documented for a desktop-only pilot, recorded as a general-use blocker. Adds the operator pilot guide, rollback plan and release checklist. Verifier **2443**, runtime **2921/34**, R1 **119**, R2 **96**; artifact `tam-os-v2.10.0.html` deterministic. **No tag, no GitHub Release.** Post-merge, the distribution question was ruled in [ADR-0002](../03b-repository-adr/ADR-0002-canonical-distribution-architecture.md) (**Accepted**): Model A (`dist/tam-os-v2.10.0.html`) is retained for the v2.10.0 controlled pilot on release-risk sequencing — **`REQUIRED` single-file dependencies = 0** — while Model B (`index.html` + assets) is approved as the preferred future architecture and deferred to the **Distribution-1** post-pilot milestone; `CLAUDE.md` is unamended and fully operative for v2.10.0. The artifact is accurately described as a **single-file application package**, not fully offline (SheetJS CDN is required for `.xlsx`; Google Fonts is cosmetic). Controlled Pilot Sign-off is now merged (`df76ec20`) and the pilot is **APPROVED but NOT YET LAUNCHED**. Next: **Controlled Pilot handoff**, then Post-Pilot Findings & Remediation |

Roadmap entries describe **intent**, not commitments to dates. A future item is a direction, not a
promise; it is authorized for implementation only when a Sprint Assignment is issued for it.
Speculative design detail does not belong here or in the architecture documents — only the sequence
does.
